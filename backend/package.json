{
  "name": "agrolease-backend",
  "version": "0.1.0",
  "private": true,
  "description": "AgroLease mobile app API. Plain, portable, containerized Node/Fastify service - no platform-specific SDK baked in (Railway dropped project-wide; long-term direction is AWS, but not part of Task 1). Runs locally now via npm run dev or its Dockerfile.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "dev": "node src/server.js",
    "start": "node src/server.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.110.2",
    "dotenv": "^17.4.2",
    "fastify": "^5.10.0"
  }
}
