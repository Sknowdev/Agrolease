import { ComingSoon } from '../../components/coming-soon/ComingSoon';

/**
 * Conduit Workspace's "Add Record" shortcut destination (Task 3's
 * dashboard redesign). Real harvest-record entry is Task 6's job -
 * this stays a Coming Soon stub per the Constitution's "Coming Soon
 * instead of hidden" rule rather than a dead/missing button.
 */
export default function AddRecordComingSoon() {
  return <ComingSoon title="Add Record" />;
}
