import { ComingSoon } from '../../components/coming-soon/ComingSoon';

/**
 * Conduit Workspace's "Agreement" shortcut destination (Task 3's
 * dashboard redesign). Real lease/agreement management is a later
 * task - this stays a Coming Soon stub per the Constitution's "Coming
 * Soon instead of hidden" rule rather than a dead/missing button.
 */
export default function AgreementComingSoon() {
  return <ComingSoon title="Agreement" />;
}
